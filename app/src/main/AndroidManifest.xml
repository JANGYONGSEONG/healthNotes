<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yongs.healthnotes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MealPlanPopup"
            android:screenOrientation="portrait"
            android:theme="@style/MyPopupTheme" />
        <activity
            android:name=".BodyCompositionPopup"
            android:screenOrientation="portrait"
            android:theme="@style/MyPopupTheme" />
        <activity android:name=".MealPlanActivity" />
        <activity android:name=".ExerciseSelectActivity" />
        <activity android:name=".ExerciseDetailActivity" />
        <activity android:name=".DoExerciseActivity" />
    </application>

</manifest>